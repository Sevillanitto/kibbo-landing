name = "education-contract-analyzer"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV namespace for the per-IP daily rate limiter (3 free analyses / IP / day)
# and the access codes (code:KIBBO-EDUCONTRACT-...). Already created and bound
# manually (education-contract-analyzer-rate-limit):
[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "b069856f23924bf8a6e84603094fbb87"
