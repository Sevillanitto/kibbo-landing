name = "delivery-contract-analyzer"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV namespace for the per-IP daily rate limiter (3 free analyses / IP / day)
# and the access codes (code:KIBBO-DELIVERY-...). Already created and bound
# manually (delivery-contract-analyzer-rate-limit):
[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "d11971d629794bfeb705e72eb42fd76a"
